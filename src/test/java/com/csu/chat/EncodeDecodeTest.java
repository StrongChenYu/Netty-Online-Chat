package com.csu.chat;

import org.junit.Test;

public class EncodeDecodeTest {


    @Test
    public void EncodeDecodeTest() {
//        PacketCodeC code = new PacketCodeC();
//        LoginRequestPacket requestPacket = new LoginRequestPacket();
//
//        ByteBuf encodeBuf = code.encode(requestPacket);
//        LoginRequestPacket packet = (LoginRequestPacket) code.decode(encodeBuf);
//
//        Assert.assertFalse(requestPacket == packet);
//        Assert.assertEquals(requestPacket, packet);
    }
}
